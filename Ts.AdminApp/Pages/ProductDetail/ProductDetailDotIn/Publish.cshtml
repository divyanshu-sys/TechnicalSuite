@page "{id:min(1)}/{isRepublish?}"
@model Ts.AdminApp.Pages.ProductDetail.ProductDetailDotIn.PublishModel
@{
    ViewBag.Title = "Publish or Unpublish ProductDetail";
}

<div class="container col-lg-12">
    <div class="page-title">
        <h3>
            ProductDetail DotIn
            <a asp-page="index" class="btn btn-sm btn-outline-info float-end me-1"><i class="fas fa-angle-left"></i> <span class="btn-header">Return</span></a>
        </h3>
    </div>
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-header"><i class="fa fa-edit"></i> @ViewBag.Title</div>
                <div class="card-body">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger rounded-3"></div>
                    <div class="row g-2">
                        <div class="mb-3 col-md-12">
                            <b>@Html.DisplayNameFor(m => m.Model.Title):</b> @Model.Model.Title
                        </div>
                    </div>
                    @if (!Model.Model.IsPublished)
                    {
                        <button class="btn btn-primary mb-1" onClick="showConfirmation('Publish', 'Confirm Publish', 'Are you sure you want to Publish?', '/productdetail/productdetaildotin/publish/@Model.Id/false')"><i class="fas fa-rocket"></i> Publish</button>
                        @if (Model.Model.PublishedById != null)
                        {
                            <button class="btn btn-warning mb-1" onClick="showConfirmation('Republish', 'Confirm Republish', 'Are you sure you want to Republish?', '/productdetail/productdetaildotin/publish/@Model.Id/true')"><i class="fas fa-redo"></i> Republish</button>
                        }
                    }
                    else
                    {
                        <button class="btn btn-danger mb-1" onClick="showConfirmation('Unpublish', 'Confirm Unpublish', 'Are you sure you want to Unublish?', '/productdetail/productdetaildotin/publish/@Model.Id/false')"><i class="fas fa-undo"></i> Unpublish</button>
                    }
                    @if (Model.Model.DeliveryPolicy.Name == DeliveryPolicyConstant.Downloadable15Days)
                    {
                        <a asp-page="updateproductdetaildocument" asp-route-id="@Model.Id" class="btn btn-secondary mb-1 float-end"><i class="fas fa-angle-double-left"></i> Upload Document</a>
                    }
                    else
                    {
                        <a asp-page="updatemainimage" asp-route-id="@Model.Id" class="btn btn-secondary mb-1 float-end"><i class="fas fa-angle-double-left"></i> Main Image</a>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
